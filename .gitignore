node_modules/
dist/
.DS_Store
*.log

# Rhodes samples — 429 MB of audio, kept out of git deliberately.
# Source of truth is Jared's local copy; the app needs them at
# src/renderer/sounds/Samples/{note}-{p|mp|mf|f}.wav
src/renderer/sounds/

# icon.png / icon.icns are committed deliverables; the iconset scratch dir is not
build/icon.iconset/
dist/
__pycache__/
*.pyc

# Machine-local Claude Code config — personal paths, not shared.
.claude/settings.local.json
